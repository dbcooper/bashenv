" Neovim-specific configuration

" _Trying_ to address Neovim (Fedora 40) error when I try to access help, but just including this plugin this doesn't do it? 😞
" Need treesitter parsers plugin or something?
" https://github.com/nvim-treesitter/nvim-treesitter?tab=readme-ov-file#installation
Plug 'nvim-treesitter/nvim-treesitter', {'do': ':TSUpdate'}
